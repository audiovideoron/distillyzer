# Database connection
DATABASE_URL=postgresql://localhost/distillyzer

# OpenAI API key (for Whisper transcription + embeddings)
OPENAI_API_KEY=sk-...

# Anthropic API key (for Claude query responses)
ANTHROPIC_API_KEY=sk-ant-...
